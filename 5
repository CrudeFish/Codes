1. Найти расстояние между двумя точками с заданными координатами (x1, y1) и (x2, y2)
#include <iostream>
using namespace std;

int x1, x2, y1, y2, x, y, sum;
float s;

int main()
{
	setlocale(LC_ALL, "Russian");
	cout << "Введите значение x1= "; cin >> x1;
	cout << "Введите значение y1= "; cin >> y1;
	cout << "Введите значение x2= "; cin >> x2;
	cout << "Введите значение y2= "; cin >> y2;
	x = x2 - x1;
	y = y2 - y1;
	sum = x + y;
	s = sqrt(sum);
	cout << "Ответ: " << s;
}

2. Даны три точки A, B, C на числовой оси. Найти длины отрезков AC и BC и их сумму.
#include <iostream>
using namespace std;

int a, b, c, ac, bc;

int main()
{
setlocale (LC_ALL, "Russian");
	cout << "Введите координату точки A: "; cin >> a;
	cout << "Введите координату точки B: "; cin >> b;
	cout << "Введите координату точки C: "; cin >> c;
	ac = (c - a);
	bc = (c - b);
	cout << "Длина отрезков AC = " << ac << ", BC = "<< bc << endl;
	cout << "Сумма отрезков = " << ac + bc;
}


3. Даны три точки A, B, C на числовой оси. Точка C расположена между точками A и B. Найти произведение длин отрезков AC и BC
#include <iostream>
using namespace std;

int a, b, c, ac, bc;

int main()
{
	setlocale (LC_ALL, "Russian");
	cout << "Введите координату точки A: "; cin >> a;
	cout << "Введите координату точки B: "; cin >> b;
	cout << "Введите координату точки C: "; cin >> c;
	ac = (c - a);
	bc = (b - c);
	cout << "Длина отрезков AC = " << ac << ", BC = "<< bc << endl;
	cout << "Произведение длин отрезков = " << ac * bc;
}

4. Даны координаты двух противоположных вершин прямоугольника:
(x1, y1), (x2, y2). Стороны прямоугольника параллельны осям координат. Найти периметр и площадь данного прямоугольника.
#include <iostream>
using namespace std;

int x1, y1, x2, y2, a, b, s, p;

int main()
{
	setlocale (LC_ALL, "Russian");
	cout << "Введите координату точки x1: "; cin >> x1;
	cout << "Введите координату точки y1: "; cin >> y1;
	cout << "Введите координату точки x2: "; cin >> x2;
	cout << "Введите координату точки y2: "; cin >> y2;
	a = (x2 - x1);
	b = (y2 - y1);
	p = (a + b) * 2;
	s = a * b;
	cout << "Периметр прямоугольника P = " << p << ", площадь прямоугольника S = " << s;
}


5. Даны координаты трех вершин треугольника: (x1, y1), (x2, y2), (x3, y3). Найти его периметр и площадь
 
#include <iostream>
using namespace std;

int x1, y1, x2, y2, x3, y3;
float a, b, c, p, s;

int main()
{
	setlocale(LC_ALL, "Russian");
	cout << "Введите координату точки x1: "; cin >> x1;
	cout << "Введите координату точки y1: "; cin >> y1;
	cout << "Введите координату точки x2: "; cin >> x2;
	cout << "Введите координату точки y2: "; cin >> y2;
	cout << "Введите координату точки x3: "; cin >> x3;
	cout << "Введите координату точки y3: "; cin >> y3;
	a = sqrt((x2 - x1) * (x2 - x1) + (y2 - y1) * (y2 - y1));
	b = sqrt((x3 - x2) * (x3 - x2) + (y3 - y2) * (y3 - y2));
	c = sqrt((x3 - x1) * (x3 - x1) + (y3 - y1) * (y3 - y1));
	p = a + b + c;
	cout << "Периметр прямоугольника P = " << p << endl;
	p = p / 2;
	s = sqrt(p * (p - a) * (p - b) * (p - c));
	cout << "Площадь прямоугольника S = " << s;
}
